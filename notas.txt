# Notas del Proyecto Get Access

## ⚠️ IMPORTANTE: TODAS LAS PRUEBAS EN PRODUCCIÓN

**SIEMPRE usar:**
- Frontend: https://getaccess.com.ar
- Scanner: https://getaccess.com.ar/scanner.html
- Backend API: https://getaccess-d3um.onrender.com (solo API/webhooks) (solo API/webhooks)

**NO usar localhost para pruebas.**

---

## ☁️ PLATAFORMAS CLOUD UTILIZADAS

| # | Servicio | Uso |
|---|---------|-----|
| 1 | Render | Hosting web + API |
| 2 | Cloudflare | DNS + SSL |
| 3 | Zoho Mail | Email soporte |
| 4 | Supabase | Base de datos PostgreSQL |
| 5 | Brevo | Envío emails transaccionales |
| 6 | MercadoPago | Pagos

---

## 📌 EMAIL CONFIGURADO (15/04/2026)

### Remitente: soporte@getaccess.com.ar ✅ FUNCIONANDO
- **Remitente emails transaccionales**: Brevo
- **Remitente emails manuales**: Zoho Mail

### Configuración DNS (Cloudflare):
- ✅ SPF: v=spf1 include:zohomail.com include:brevo.com ~all
- ✅ DKIM: zoho._domainkey + brevo1._domainkey + brevo2._domainkey
- ✅ DMARC: _dmarc

### Remitentes en Brevo:
- ✅ soporte@getaccess.com.ar (autentificado)
- ✅ getaccess.com.ar (dominio autentificado)

### Variables en Render:
```
BREVO_SENDER_EMAIL = soporte@getaccess.com.ar
```

---

## ☁️ PLATAFORMAS CLOUD UTILIZADAS

| # | Servicio | Uso |
|---|---------|-----|
| 1 | Render | Hosting web + API |
| 2 | Cloudflare | DNS + SSL |
| 3 | Zoho Mail | Email soporte |
| 4 | Supabase | Base de datos PostgreSQL |
| 5 | Brevo | Envío emails transaccionales |
| 6 | MercadoPago | Pagos |

---

## 📌 DOMINIO CUSTOM CONFIGURADO

### getaccess.com.ar ✅ FUNCIONANDO
- Dominio configurado con Cloudflare (DNS)
-托管 en Render (getaccess-d3um.onrender.com)
- SSL certificado emitido
- Redirect www.getaccess.com.ar → getaccess.com.ar

### Configuración DNS Cloudflare:
- Nameservers: marvin.ns.cloudflare.com, sonia.ns.cloudflare.com
- CNAME: @ → getaccess-d3um.onrender.com
- CNAME: www → getaccess-d3um.onrender.com

---

## 📌 EMAIL CONFIGURADO

### soporte@getaccess.com.ar ✅ FUNCIONANDO
- **Remitente emails transaccionales**: Brevo (send-api)
- **Remitente emails manuales**: Zoho Mail
- Webmail Zoho: https://mail.zoho.com
- Login Zoho: soporte@getaccess.com.ar
- Plan: Free (hasta 5 usuarios, 5GB)

### Configuración DNS (Cloudflare):
- MX: mx.zoho.com (10), mx2.zoho.com (20), mx3.zoho.com (50)
- SPF: v=spf1 include:zohomail.com include:brevo.com ~all
- DKIM: zoho._domainkey → verificado
- Brevo DKIM: brevo1._domainkey, brevo2._domainkey → verificado
- Brevo DMARC: _dmarc → verificado

### Remitentes en Brevo:
- soporte@getaccess.com.ar ✅ Autentificado
- getaccess.com.ar ✅ Dominio autentificado

---

## 📌 FIRMA EMAIL CONFIGURADA (pendiente imagen)

### Firma actual:
--
Get Access - Sistema de Entradas
> soporte@getaccess.com.ar
> https://getaccess.com.ar
> Tu acceso a los mejores eventos

### Pendiente: Agregar logo en imagen
- Subir logo a URL pública (Imgur o Cloudflare)
- Configurar firma con imagen en Zoho

---

## 📌 PROYECTO PRIORITARIO ACTUAL: Migración a PostgreSQL - Phase de Testing

### Estado: 🧪 Probando todas las funcionalidades post-migración

**IMPORTANTE:** Estamos probando todas las funcionalidades debido a la migración de SQLite a PostgreSQL (Supabase). Puede haber errores de compatibilidad.

### Solución implementada
- **Migración a Supabase PostgreSQL** completada
- Los datos ahora persisten entre deploys
- Conexión: postgresql://postgres.xgwbcepopluehupublkz:*@aws-1-sa-east-1.pooler.supabase.com:5432/postgres

### Correcciones aplicadas durante testing:
- ✅ Fix login - compatibilidad dict/tupla
- ✅ Fix categorias - compatibility with PostgreSQL tuples
- ✅ Fix transferencias - usuario_destino es INTEGER, no email
- ✅ Fix eventos - PostgreSQL tuples
- ✅ Fix lastrowid - added SELECT lastval() for PostgreSQL

### Estado de funcionalidades implementadas
- ✅ Carrito de compras con MercadoPago
- ✅ Compra real con webhook de MP
- ✅ Código único por entrada (GA-XXXXXX)
- ✅ QR en email de entrada
- ✅ Botón Carrito solo visible para usuarios logueados
- ✅ Base de datos persistente (Supabase PostgreSQL)

---

## ⚠️ IMPORTANTE: TODAS LAS PRUEBAS EN PRODUCCIÓN

**SIEMPRE usar:**
- Frontend: https://getaccess.com.ar
- Scanner: https://getaccess.com.ar/scanner.html
- Backend API: https://getaccess-d3um.onrender.com (solo API/webhooks)

**NO usar localhost para pruebas.**

---

## Fecha Última Actualización: Miércoles 15 de Abril de 2026 - Configuración Email

---

## 📝 PENDIENTE DE RECORDAR

- Crear evento "Evento Prueba" - ✅ YA CREADO (ID=9)

---

## 🔧 FALLAS CORREGIDAS HOY (13/04/2026)

1. Fix verificado: False → 0 (PostgreSQL integer)
2. Fix columnas usadas/transferida en INSERT entradas
3. Fix generar código GA- solo cuando pago confirmado (no en pendiente)
4. Fix email QR: usar quickchart.io URL externa (no base64 embebido)
5. Fix pasar entrada_id en webhook email
6. Fix código GA-10chars (alfanumérico)
7. Fix validar entrada por preference_id (no por ID)
8. Fix cantidad en validaciones
9. Fix scanner solo admin (mostrar alert)
10. Fix estilos botón login scanner (gradiente, btn-block)
11. Fix spinner y loading en login scanner
12. Corregir password usuario aantoniaa1982@gmail.com
13. Fix duplicate loading (declarar loading dos veces) → usar loginLoading
14. Fix código residual en login (duplicado)

---

## 📋 CIRCUITO COMPLETO - ESTADO ACTUAL

| Funcionalidad | Estado |
|-------------|--------|
| Registro/Login | ✅ |
| Ver eventos | ✅ |
| Compra + MercadoPago | ✅ |
| Email con QR | ✅ (quickchart.io) |
| Reenviar ticket | ✅ |
| Código GA-10chars | ✅ |
| Mis entradas (solo pagadas) | ✅ |
| Scanner QR | ✅ (solo admin) |
| Scanner: validar entrada | ✅ |
| Webhook: genera código | ✅ |
| Analytics | ⏳ |
| Transferencia | ⏳ |

---

## 🔜 PENDIENTE PARA MAÑANA

### Circuitos a probar (PRUEBA COMPLETA):
⚠️ IMPORTANTE: Usar email REAL (noadmin@getaccess.com) para crear evento, y email distinto para comprar.

1. [ ] Crear evento nuevo (desde admin)
2. [ ] Crear usuario de prueba (registro + verificar email)
3. [ ] Login con nuevo usuario
4. [ ] Compra real con MercadoPago (tarjeta prueba: 4509 9535 6621 0551)
5. [ ] Verificar webhook confirma pago
6. [ ] Recibir email con QR (ver diseño)
7. [ ] Ver en Mis Entradas
8. [ ] Reenviar ticket por email
9. [ ] Scanner: validar entrada
10. [ ] Transferir entrada a otro usuario
11. [ ] Aceptar transferencia
12. [ ] Analytics: ver ventas

### URLs para probar (NUEVO DOMINIO):
- Frontend: https://getaccess.com.ar
- Scanner: https://getaccess.com.ar/scanner.html
- Backend API: https://getaccess-d3um.onrender.com/api (solo API/webhooks)
- (También disponible en: https://getaccess-d3um.onrender.com)

### Credenciales:
- Admin: admin@getaccess.com / admin123
- Usuario prueba: guilles10@yahoo.com.ar / guille123
- Antonia: aantoniaa1982@gmail.com / guille123

### Issues a resolver:
- [ ] Auto-deploy en Render (configurar webhook)
- [ ] Toast no aparece en scanner (usar alert por ahora)

---

## 📋 RESUMEN DE TRABAJO HECHO

### Funcionalidades implementadas:
- ✅ **Carrito de compras** con MercadoPago
- ✅ **Pagos reales** a través de MercadoPago (webhook configurado)
- ✅ **Código único** por entrada (formato GA-XXXXXX)
- ✅ **QR Code** en email de confirmación
- ✅ **Validación de fecha** de eventos (no permite fechas pasadas)
- ✅ **Botón Carrito** solo visible para usuarios logueados

### Problemas encontrados:
- ⚠️ **Base de datos efímera en Render** - SQLite se borra con cada deploy
- ⚠️ Los eventos creados se pierden al hacer deploy del código

---

## 🚀 DEPLOY EN RENDER

### URL del servicio
- **Backend API**: https://getaccess-d3um.onrender.com (solo API/webhooks)
- **Frontend**: https://getaccess.com.ar (dominio custom)

### Estado actual: 🔧 EN DEBUG

**Problema actual:** Las rutas API devuelven 404 en producción
- `/api/eventos/` → 404
- `/health` → funciona (devuelve JSON)
- `/` → funciona (sirve index.html)
- Pero cualquier ruta `/api/*` devuelve 404

**Última acción:** Se agregó debug output en el código para ver los paths en los logs de Render.

### Cómo ver los logs de Render:
1. Ir al dashboard de Render: https://dashboard.render.com
2. Click en el servicio "GetAccess"
3. Click en "Logs" (a la izquierda)
4. Buscar líneas que digan "DEBUG" para ver los paths

### Variables de entorno configuradas en Render:
```
SECRET_KEY = access_on_super_secret_key_2026_fallback
BREVO_API_KEY = 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
BREVO_SENDER_EMAIL = support.getaccess@gmail.com
MERCADO_PAGO_ACCESS_TOKEN = APP_USR-2888302331727804-031609-eb4c51fc6c1654d701d4a5f3b24fbcd7-1921694
ALLOWED_ORIGINS = https://getaccess-d3um.onrender.com
```

### Pasos para debuggear:
1. Esperar a que redepliegue
2. Ver los logs en busca de "DEBUG"
3. Verificar que FRONTEND_DIR y BACKEND_DIR estén correctos
4. Si los paths están bien, el problema puede estar en el orden de las rutas de FastAPI

### Código del Procfile (backend/):
```
web: uvicorn main:app --host 0.0.0.0 --port $PORT
```

### Requirements.txt (backend/):
```
fastapi
uvicorn
sqlalchemy
pydantic[email]
python-jose[cryptography]
PyJWT
passlib[bcrypt]
bcrypt
python-multipart
httpx
requests
qrcode
Pillow
```

---

## GIT - Control de Versiones

### Repositorio
- **GitHub**: https://github.com/SupportGetAccess/GetAccess
- **Rama actual**: main
- **Estado**: al día con origin/main

### Comandos útiles
```bash
# Ver estado
git status

# Ver historial
git log --oneline

# Ver ramas
git branch

# Agregar y hacer commit
git add .
git commit -m "mensaje"

# Subir a GitHub
git push

# Ver un commit específico
git show HASH

# Crear tag de versión
git tag v1.0.0 -m "Versión 1.0"
git push origin v1.0.0
```

---

## ESTADO ACTUAL DEL PROYECTO

### 🔧 Bug Fix - Error 500 en Mis Entradas

**Problema:** Errores 500 Internal Server Error al navegar a "Mis Entradas"
- GET /api/entradas/ devolvía 500
- GET /api/transferencias/pendientes devolvía 500

**Causa:** Error de threading de SQLite
- Mensaje: "SQLite objects created in a thread can only be used in that same thread"
- Uvicorn usa múltiples hilos para manejar requests

**Solución:** Agregar `check_same_thread=False` a la conexión SQLite
```python
def get_db():
    conn = sqlite3.connect(DATABASE_URL, check_same_thread=False)
    conn.row_factory = sqlite3.Row
    try:
        yield conn
    finally:
        conn.close()
```

---



### ✅ Implementado y Funcional

#### 🔄 Sistema de Transferencia de Entradas
**Funcionalidades:**
- Transferir entrada a otro email
- Confirmación de seguridad antes de transferir (2 pasos)
- El destinatario recibe un email con código de transferencia
- La entrada desaparece del transferidor hasta que se acepte
- Banner amarillo en Mis Entradas si hay transferencias pendientes
- Aceptar transferencia con código
- Vista dedicada para aceptar transferencias

**APIs:**
- POST /api/entradas/{id}/transferir → Iniciar transferencia
- POST /api/entradas/aceptar-transferencia → Aceptar con token
- GET /api/entradas/{id}/transferencias → Ver historial
- GET /api/transferencias/pendientes → Ver transferencias para aceptar

**Tablas:**
- transferencias: id, entrada_id, usuario_origen, usuario_destino, token, estado, created_at, accepted_at
- entradas: columna "transferida" (0 o 1)

---

#### 📴 Modo Offline Scanner
**Funcionalidades:**
- Detecta conexión online/offline automáticamente
- Cachea códigos válidos del evento
- Valida sin conexión usando caché
- Cola de validaciones offline
- Sincroniza cuando vuelve la conexión
- Indicador visual de estado en header

**APIs:**
- GET /api/eventos/{id}/codigos-validos → Códigos para offline

---

#### 📊 Analytics Avanzado
**Funcionalidades:**
- Dashboard con métricas globales
- Ventas por categoría (gráfico)
- Top eventos por ventas
- Tendencia de ventas por día
- Proyección de ventas (probabilidad de agotar, tendencia)
- Selector de evento específico o todos

**APIs:**
- GET /api/analytics/todos → Métricas globales
- GET /api/analytics/evento/{id} → Métricas por evento

---

#### Sistema de Validación de Entradas (Scanner QR)
**Funcionalidades:**
- Escanear QR con la cámara del dispositivo
- Subir imagen QR como alternativa a la cámara
- Validar si la entrada es real y no ha sido usada
- Mostrar estados: ✅ Válida, ❌ Ya usada, ⚠️ Inválida
- Estadísticas en Tiempo Real
- Historial de escaneos (últimos 7 días)
- Entrada manual de código

**APIs:**
- POST /api/validar-entrada → Valida y marca como usada
- GET /api/validar/{codigo} → Solo consulta sin marcar
- GET /api/eventos/{id}/estadisticas → Entradas usadas/disponibles
- GET /api/validaciones/historial → Historial de escaneos (7 días)

---

## 📋 SCANNER QR - ESTADO ACTUAL

### URLs de acceso:
- **PC (HTTP)**: http://localhost:3000/scanner.html
- **Celular (HTTPS)**: https://192.168.1.40:3443/scanner.html
- **Backend HTTP**: http://localhost:8000
- **Backend HTTPS**: https://192.168.1.40:8443

### Funcionalidades del Scanner:
- ✅ Selección manual de evento (obligatoria)
- ✅ Estadísticas del evento seleccionado
- ✅ Historial de validaciones (7 días)
- ✅ Cámara (html5-qrcode)
- ✅ Subir imagen QR
- ✅ Entrada manual de código
- ✅ QRbox dinámico (70% del viewfinder)
- ✅ Diseño visual mejorado con resultado en paralelo al scanner
- ✅ Modo Offline con sincronización

---

## 🏗️ ESTRUCTURA DEL PROYECTO

```
C:\Users\guill\eventos_tickets_full\
├── backend/
│   ├── main.py              # API FastAPI
│   ├── access_on.db         # Base de datos SQLite
│   ├── serve_https.py       # Servidor HTTPS
│   └── crear_y_enviar_ticket.py  # Script para crear y enviar tickets
├── frontend/
│   ├── index.html          # App web principal
│   ├── scanner.html         # App de validación QR
│   ├── serve.py            # Servidor HTTP + HTTPS
│   ├── cert.pem            # Certificado SSL
│   └── key.pem             # Clave SSL
├── notas.txt               # Este archivo
├── AGENTS.md              # Reglas para agentes IA
└── DER_GetAccess.jpg      # Diagrama entidad-relación
```

---

## 🔌 APIs DISPONIBLES

### Autenticación
- POST /api/auth/registro - Registro
- POST /api/auth/login - Login
- POST /api/auth/verificar - Verificar email
- GET /api/auth/me - Datos del usuario actual
- POST /api/auth/solicitar-reset - Solicitar recuperación password
- POST /api/auth/reset-password - Recuperar password

### Eventos
- GET /api/eventos/ - Listar eventos
- GET /api/eventos/{id} - Detalle de evento
- GET /api/eventos/categorias - Categorías
- POST /api/eventos/ - Crear evento (admin)

### Entradas
- POST /api/entradas/ - Comprar entrada
- GET /api/entradas/ - Mis entradas (excluye transferidas)
- POST /api/entradas/{id}/transferir - Transferir entrada
- POST /api/entradas/aceptar-transferencia - Aceptar transferencia

### Transferencias
- GET /api/transferencias/pendientes - Ver transferencias por aceptar

### Validación
- POST /api/validar-entrada - Validar entrada (marca como usada)
- GET /api/validar/{codigo} - Consultar estado
- GET /api/eventos/{id}/estadisticas - Estadísticas
- GET /api/validaciones/historial - Historial
- GET /api/eventos/{id}/codigos-validos - Códigos para offline

### Analytics
- GET /api/analytics/todos - Métricas globales
- GET /api/analytics/evento/{id} - Métricas por evento

### Pagos
- POST /api/pagos/crear-preferencia - Crear preferencia MP
- POST /api/pagos/webhook - Webhook MercadoPago

### Email
- POST /api/email/enviar-ticket - Reenviar ticket por email

---

## 🗄️ BASE DE DATOS

### Tablas:
- **usuarios**: id, email, nombre, apellido, password, verificado, codigo_verificacion, rol
- **eventos**: id, nombre, descripcion, fecha, lugar, precio, capacidad, vendidos, imagen, categoria
- **entradas**: id, evento_id, usuario_id, cantidad, total, estado, preference_id, payment_id, creada_en, usada, transferida
- **evento_imagenes**: id, evento_id, url, orden
- **validaciones**: id, entrada_id, scanner_id, cantidad_original, timestamp
- **transferencias**: id, entrada_id, usuario_origen, usuario_destino, token, estado, created_at, accepted_at
- **password_reset**: id, email, token, usado, expires_at

---

## 🚀 COMANDOS

### Iniciar todos los servicios:
```powershell
powershell -Command "Stop-Process -Name python -Force -ErrorAction SilentlyContinue"
powershell -Command "Start-Process python -ArgumentList '-m','uvicorn','main:app','--host','0.0.0.0','--port','8000' -WorkingDirectory 'C:\Users\guill\eventos_tickets_full\backend'"
powershell -Command "Start-Process python -ArgumentList 'C:/Users/guill/eventos_tickets_full/frontend/serve.py'"
```

### Verificar servicios:
```powershell
powershell -Command "Invoke-WebRequest -Uri 'http://localhost:8000/health' -UseBasicParsing | Select-Object -ExpandProperty StatusCode"
powershell -Command "Invoke-WebRequest -Uri 'http://localhost:3000/' -UseBasicParsing | Select-Object -ExpandProperty StatusCode"
```

---

## PENDIENTE / MEJORAS

1. ✅ Sistema de validación QR - IMPLEMENTADO
2. ✅ Historial de validaciones - IMPLEMENTADO
3. ✅ Acceso desde celular (HTTPS) - IMPLEMENTADO
4. ✅ Transferencia de entradas - IMPLEMENTADO
5. ✅ Modo offline scanner - IMPLEMENTADO
6. ✅ Analytics avanzado - IMPLEMENTADO
7. ✅ Seguridad - Secrets en variables de entorno - IMPLEMENTADO
8. ✅ CORS restringido a orígenes permitidos - IMPLEMENTADO

---

## 🔧 VARIABLES DE ENTORNO

Para ejecutar el backend con variables de entorno configuradas:

```powershell
$env:SECRET_KEY = "tu_secret_key_aqui"
$env:BREVO_API_KEY = "tu_brevo_api_key"
$env:BREVO_SENDER_EMAIL = "tu_email@ejemplo.com"
$env:MERCADO_PAGO_ACCESS_TOKEN = "tu_mp_access_token"
$env:MERCADO_PAGO_WEBHOOK_SECRET = "tu_webhook_secret"
$env:ALLOWED_ORIGINS = "http://localhost:3000,https://192.168.1.40:3443"

python -m uvicorn main:app --host 0.0.0.0 --port 8000
```

Valores por defecto (fallback) incluidos en el código para desarrollo.

---

## 👤 CREDENCIALES

- **Admin**: admin@getaccess.com / admin123
- **Usuario prueba**: guilles10@yahoo.com.ar / guille123

---

## NOTAS IMPORTANTES

- **REGLA AGENTS.MD**: Cada vez que se modifique el frontend, se deben reiniciar TODOS los servicios
- El scanner requiere seleccionar un evento manualmente
- Las estadísticas muestran solo el evento seleccionado
- Historial muestra últimos 7 días
- Las entradas transferidas no aparecen en "Mis Entradas" hasta que se aceptan
- El código de transferencia se envía por email al destinatario

---

## 🚀 ROADMAP - FUNCIONALIDADES PARA SUPERAR COMPETENCIA

### Funcionalidades PRIORITARIAS (Alto Impacto UX)

| # | Funcionalidad | Estado | Prioridad |
|---|---------------|--------|-----------|
| 1 | **Búsqueda de entradas por email/DNI** (sin login) | ⬜ | 🔥🔥🔥 |
| 2 | **Carrito de compras** (múltiples eventos) | ⬜ | 🔥🔥🔥 |
| 3 | **Wallet/credits** (crédito prepagado) | ⬜ | 🔥🔥 |
| 4 | **Lotes de precio dinámico** (early bird, VIP) | ⬜ | 🔥🔥 |
| 5 | **Gift cards / Regalos** | ⬜ | 🔥🔥 |

### Funcionalidades de ENGAGEMENT

| # | Funcionalidad | Estado |
|---|---------------|--------|
| 6 | **Guardar eventos favoritos** | ⬜ |
| 7 | **Wishlist con alertas** | ⬜ |
| 8 | **Historial de eventos asistidos** | ⬜ |
| 9 | **Recomendaciones personalizadas** | ⬜ |
| 10 | **Notificaciones push** | ⬜ |

### Funcionalidades para ORGANIZADORES

| # | Funcionalidad |
|---|---------------|
| 11 | **Invitar colaboradores con roles** |
| 12 | **Mapas interactivos de asientos** |
| 13 | **Código QR con branding del evento** |

---

### 🗓️ Roadmap Sugerido

**Fase 1 (Inmediata):**
- ✅ Búsqueda de entradas por email/DNI sin login - IMPLEMENTADO
- ✅ Carrito de compras - IMPLEMENTADO

**Fase 2:**
- Gift cards / Regalos
- Guardar favoritos
- Notificaciones push

**Fase 3:**
- Lotes de precio dinámico
- Mapas de asientos
- Wallet digital

---

## 🔜 PENDIENTE PARA LA PRÓXIMA SESIÓN

### Tareas Inmediatas:
- [ ] Subir logo a URL pública (Imgur o Cloudflare)
- [ ] Configurar firma con imagen en Zoho Mail

### Circuitos a probar (desde notas anteriores):
1. [ ] Crear evento nuevo (desde admin)
2. [ ] Crear usuario de prueba (registro + verificar email)
3. [ ] Login con nuevo usuario
4. [ ] Compra real con MercadoPago (tarjeta prueba: 4509 9535 6621 0551)
5. [ ] Verificar webhook confirma pago
6. [ ] Recibir email con QR (ver diseño)
7. [ ] Ver en Mis Entradas
8. [ ] Reenviar ticket por email
9. [ ] Scanner: validar entrada
10. [ ] Transferir entrada a otro usuario
11. [ ] Aceptar transferencia
12. [ ] Analytics: ver ventas

### Issues a resolver:
- [ ] Auto-deploy en Render (configurar webhook)
- [ ] Toast no aparece en scanner (usar alert por ahora)

---

---

## 🛠️ MANTENER SERVICIO ACTIVO (Evitar cold start)

### Opción: UptimeRobot (recomendado - gratis)
1. Ir a https://uptimerobot.com
2. Agregar monitor: https://getaccess-d3um.onrender.com/health
3. Intervalo: cada 15 min (plan gratuito)

### Alternativa: PowerShell local
```powershell
$action = New-ScheduledTaskAction -Execute "powershell" -Argument "-Command Invoke-WebRequest -Uri 'https://getaccess-d3um.onrender.com/health' -UseBasicParsing"
$trigger = New-ScheduledTaskTrigger -Once -At "09:00" -RepetitionInterval (New-TimeSpan -Minutes 10)
Register-ScheduledTask -Action $action -Trigger $trigger -TaskName "PingGetAccess"
```

---

## 🗄️ ADMINISTACIÓN BASES DE DATOS

### Limpiar tokens de recuperación (Supabase SQL):
```sql
DELETE FROM password_reset;
```

### Ver tokens pendientes:
```sql
SELECT * FROM password_reset;
```

---

## 📝 PENDIENTE

- [ ] UptimeRobot configurado
- [ ] Limpiar tokens pruebareset (cuenta aantoniaa1982@gmail.com)

---

*Para más detalles ver análisis de competidores en las notas de sesión.*
